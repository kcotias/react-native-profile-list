import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const App = props => (
  <View>
    <Text>App</Text>
  </View>
);

const styles = StyleSheet.create({
  container: {
    flex: 1
  }
});

export default App;
